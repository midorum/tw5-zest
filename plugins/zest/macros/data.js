/*\
title: $:/plugins/midorum/zest/macros/data.js
type: application/javascript
module-type: macro
\*/
(function () {
  "use strict";
  // exports.name = "zest-d";
  // exports.params = [{ name: "q" }, { name: "t0" }, { name: "t1" }, { name: "w" }];
  // exports.run = (q, t0, t1, w) => {
  //   if (!q || !t0) return 'invalid syntax'
  //   w = w && w.getTiddler ? w : $tw.rootWidget.wiki
  //   const i = w.extractTiddlerDataItem(`$:/plugins/midorum/zest/data/${q}`, t0)
  //   if (!i) return 'unknown type'
  //   return !t1 ? i
  //     : t1[0] === 'i' ? i + '/' + new Date().getTime()
  //       : t1[0] === '!' ? w.getTiddler(i)?.getFieldString(t1.slice(1))
  //         : t1[0] === '#' ? w.extractTiddlerDataItem(i, t1.slice(1))
  //           : 'unknown type'
  // }
})();